// Define the `phonecatApp` module
var main = angular.module('main', []);

// Define the `PhoneListController` controller on the `main` module
main.controller('PhoneListController', function PhoneListController($scope) {
  console.log('hello')
});